import type { GoogleData } from '@/types';

export async function getPlaceData(_businessName: string, _url: string): Promise<GoogleData> {
  // TODO: Google Places findplacefromtext + details
  console.log('getPlaceData stub called');
  throw new Error('Not implemented — use mock data');
}
